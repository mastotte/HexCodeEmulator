Understand Program counter and how changing its value moves the emulator forward
Setting up a class containing the all the CPU instructions as functions so that they can be stored in an array and called as the .slug file is read
Adding program counter and the registers to the class 

controller addresses
gui - image given by GPU

gpu - reads from VRAM and shows image

our gpu is just storing a frame buffer (the next image to be shown)


g++ -o main file.cpp file.cpp -I(directory name) 
can include an h file that is in a different directory from the c files


g++ main.cpp -c -o main.o 
